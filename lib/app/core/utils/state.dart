import 'dart:convert';

import 'package:flutter/services.dart';

class StateJson {
  // static Future<List<CountryCode>> readJson() async {
  //   final String response =
  //   await rootBundle.loadString('assets/country_iso.json');
  //   final list = json.decode(response) as List<dynamic>;
  //
  //   return list.map((e) => CountryCode.fromJson(e)).toList();
  // }
  //
  // static Future<List<CountryJson>> countryCode() async {
  //   final String response = await rootBundle.loadString('assets/countries.json');
  //   final list = json.decode(response) as List<dynamic>;
  //
  //   return list.map((e) => CountryJson.fromJson(e)).toList();
  // }
}
