// Route guards are reserved for future auth checks.
// Currently the app assumes a valid token.

class RouteGuards {
  RouteGuards._();

  // Placeholder for auth guard logic
  static bool isAuthenticated() => true;
}
